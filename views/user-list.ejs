<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ポイントUP勤怠</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <%- include('header'); %>
    <main>
      <ul class="list">
        
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>ユーザー名</th>
                        <th>Email</th>
                        <th>ボタン</th>
                    </tr>
                </thead>
                <tbody>
                  <% users.forEach((user) => { %>
                    <tr>
                        <td class="tbl-id"><%=user.id%></td>
                        <td><%=user.name%></td>
                        <td><%=user.email%></td>
                        <td class="a-center"><a href="/user/<%= user.id %>"  class="detail-btn">詳細</a></td>
                    </tr>
                  <% }); %>
                </tbody>
            </table>
        
      </ul>
    </main>
  </body>
</html>